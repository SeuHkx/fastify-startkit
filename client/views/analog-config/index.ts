// auto scaffolded page script (TS) for analog-config
(function(){
  (window as any).PageScripts = (window as any).PageScripts || {};
  (window as any).PageScripts['analog-config'] = function(Alpine: any, root: Document){
    Alpine.data('analogConfig', () => ({
      message: 'analog-config ready',
    }));
  };
})();
