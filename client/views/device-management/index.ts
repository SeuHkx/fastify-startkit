// auto scaffolded page script (TS) for device-management
(function(){
  (window as any).PageScripts = (window as any).PageScripts || {};
  (window as any).PageScripts['device-management'] = function(Alpine: any, root: Document){
    Alpine.data('deviceManagement', () => ({
      message: 'device-management ready',
    }));
  };
})();
