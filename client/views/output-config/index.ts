// auto scaffolded page script (TS) for output-config
(function(){
  (window as any).PageScripts = (window as any).PageScripts || {};
  (window as any).PageScripts['output-config'] = function(Alpine: any, root: Document){
    Alpine.data('outputConfig', () => ({
      message: 'output-config ready',
    }));
  };
})();
